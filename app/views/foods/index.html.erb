<h1>Hi, <%= current_user.name.capitalize %> this is your Foods</h1>

<div id="foods">
    <table>
      <tr>
        <th>Name:</th>
        <th>Measurement Unit:</th>
        <th>Price:</th>
        <th>Actions: </th>
      </tr>
      <% @foods.each do |food| %>
      <tr>
        <td><%= food.name %></td>
        <td><%= food.measurement_unit %></td>
        <td><%= food.price %></td>
        <td><%= button_to "Destroy this food", @food, method: :delete %></td>
      </tr>
      <% end %>
    </table>
</label>
</div>
